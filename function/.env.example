# AWS Credentials (für lokales Testen)
# Kopiere diese Datei zu ".env" und fülle die Werte aus
# WICHTIG: .env ist in .gitignore und wird NICHT commited!

# S3 Bucket Name (erstellt in Schritt 1)
S3_BUCKET_NAME=mein-logging-bucket-2024

# AWS Region
AWS_REGION=eu-central-1

# AWS Credentials (optional - falls nicht in ~/.aws/credentials)
# AWS_ACCESS_KEY_ID=dein_access_key
# AWS_SECRET_ACCESS_KEY=dein_secret_key

# API Gateway URL (nach Deployment)
API_GATEWAY_URL=https://xxxxxxxxxx.execute-api.eu-central-1.amazonaws.com/prod/log
